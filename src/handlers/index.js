const CalloutResponseHandler = require('./CalloutResponseHandler')
const CompareResponseHandler = require('./CompareResponseHandler')
const ResponseHandler = require('./ResponseHandler')

module.exports = {
  CalloutResponseHandler,
  CompareResponseHandler,
  ResponseHandler
}
